import { SlideArrowDirective } from './slide-arrow.directive';

describe('SlideArrowDirective', () => {
  it('should create an instance', () => {
    const directive = new SlideArrowDirective();
    expect(directive).toBeTruthy();
  });
});
